---
import { locales, defaultLocale } from "@i18n/utils.ts"
const localesJSON = JSON.stringify(locales)
const fallback = defaultLocale
---

<!doctype html>
<html lang={fallback}>
  <head>
    <meta charset="utf-8" />
    <script define:vars={{ locales, fallback }}>
      const code = navigator.language.split("-")[0]
      window.location.replace(`/${locales.includes(code) ? code : fallback}/use`)
    </script>
  </head>
  <body></body>
</html>
